Task 1 Data

Steps:
-Read the source file as text
-Convert Ruby-like hash keys into a Python-readable format
-Insert them into the `raw_books` table
-uild the `books_summary` table inside the database using SQL

